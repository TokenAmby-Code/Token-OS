// Typed contracts for the ops cockpit — now sourced from the shared
// `@token-os/contracts` package (token-api/web/contracts/) so the cockpit and
// the discord daemon compile against the same read-model contracts.
// This thin re-export keeps every existing `./types` import working.
export * from '@token-os/contracts';

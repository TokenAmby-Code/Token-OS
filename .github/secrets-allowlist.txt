# Secrets scan allowlist — one substring per line
# Add false positives here so CI stops flagging them
#
# Format: any line in the scan output containing this substring is allowed
# Lines starting with # are comments

# Example/placeholder IPs in documentation templates
# (Add specific false positives as they arise)

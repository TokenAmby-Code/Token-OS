# Staged `.macro` Files Archive

Archived 2026-05-09 during the official-schema cleanup.

These are old import staging files, not canonical source. Use fresh official `.macro` JSON generated from `macrodroid-llm-schema.yaml` or extracted from `EXPORT.mdr`.

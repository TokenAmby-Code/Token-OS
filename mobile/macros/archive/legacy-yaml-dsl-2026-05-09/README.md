# Legacy YAML DSL Archive

Archived 2026-05-09.

These files were written for the pre-official custom `macrodroid-gen` YAML DSL. They are not active inputs anymore. The official `macrodroid-llm-schema.yaml` supersedes this format.

Do not add new YAML specs here. Generate/import official `.macro` JSON wrapper files instead.

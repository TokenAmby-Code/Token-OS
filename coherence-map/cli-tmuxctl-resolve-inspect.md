# Coherence Map — cli-tmuxctl-resolve-inspect

Wave 1 read-only map for tmuxctl pane resolution / inspection / labelling / focus / occupancy / id-sanitization.

Scope read:
- Tests: `cli-tools/tests/test_tmuxctl_focus.py`, `test_tmuxctl_focus_guard.py`, `test_tmuxctl_labels.py`, `test_tmuxctl_layout.py`, `test_tmuxctl_occupancy.py`, `test_tmuxctl_pane_select.py`, `test_tmuxctl_resolve_instance.py`, `test_tmuxctl_resolver_public.py`, `test_tmuxctl_inspect_canonical.py`, `test_tmuxctl_public_ids.py`, `test_tmuxctl_metal_resolver.py`, `test_tmuxctl_metal_restart.py`, `test_tmuxctl_liveness.py`, `test_tmuxctl_freelist.py`, `test_tmux_pane_label.py`, `test_tmux_shim_id_sanitizer.py`.
- Production: `tmuxctld/lib/tmuxctl/{resolver,inspect,public_ids,occupancy,liveness,focus,focus_guard,pane_select,layout,labels,builder,invariants,metal_resolver,metal_restart,service,tmux_adapter}.py`, `cli-tools/bin/{tmuxctl,tmux,tmux-pane-label,tmux-grid-expand}`.
- Vault refs: `/Volumes/Imperium/Imperium-ENV/Sessions/coherence-wave.md`, Mars/Architecture and Mars/Bugs/Tasks refs cited below.

## 1. Existing-test audit

| Test file | Behavior pinned | Assertion quality / smell |
|---|---|---|
| `test_tmuxctl_focus.py` | Pins grid focus plan stashing only grid siblings, preserving side panes; H-layout palace stashes one center sibling; side focus resizes side pane without stash/topology mutation; grid and side focus state can coexist; `tmux-grid-expand` stays `resize-pane -Z` only; grid unfocus avoids `select-layout tiled`; `cleanup_transient_windows` recognizes `_stash_`, `_fstash_`, `_focus_stash_` families. | Mostly real characterization of `focus.py` plan output (`tmuxctld/lib/tmuxctl/focus.py:128-195`, `198-311`) and `revert.py` cleanup. One brittle/static smell: `test_expand_script_has_no_topology_or_audience_control_path` is token-string scanning of a shell script, so comments/help text can false fail/pass. Fake adapter does not model real tmux pane movement; tests validate planned argv, not runtime topology. |
| `test_tmuxctl_focus_guard.py` | Pins focus preservation: restores displaced automation, skips restore for camera-neutral operations, treats `select-pane -Z -T` and `select-pane -P` as camera-mutating, preserves/reapplies zoom, cedes to human `client_activity`, redirects logs in tests, resolves log paths at write time, separates mechanicus/general logs, rolls oversized logs, and does not mask body exceptions. | Stronger than ordinary mocks: `FakeTmuxServer` exercises real `TmuxAdapter` classification/counter through patched subprocess (`tmux_adapter.py:296-352`, `448-489`) and `focus_guard.py:154-280`. Still a model, not tmux integration; timing/log rollover uses filesystem and monkeypatched stat-ish state but no sleeps. Good regression value for prior rubber-band failures. |
| `test_tmuxctl_labels.py` | Pins canonical role collapse: palace legacy six-seat labels collapse to H-layout `W/E/N/S`; somnium left column collapses to `W` while right grid `NE/SE` survives. | Small, real pure-function assertions over `labels.canonical_pane_role`; no obvious tautology. Coverage is intentionally narrow and does not assert unknown/malformed labels. |
| `test_tmuxctl_layout.py` | Pins canonical layout ratios/widths, phone-width scaling, somnium grid cell equality, ratio drift rejection, builder side-column postcondition strictness/recovery relaxation, typed `PaneRole` canonicalization and raw `%NN` rejection, required-seat and grid-cardinality invariants. | Real pure/model assertions across `layout.py`, `builder.py`, `invariants.py`, `models.py`. Some tests pin exact pixel constants (`DETACHED_W`, 239/71/95), which are good regression anchors but brittle if product intentionally resizes. |
| `test_tmuxctl_occupancy.py` | Pins `assert_dispatch_target_available`: singleton labels are refused even with empty `@INSTANCE_ID`; clean worker with no instance/live-agent is allowed. | Real behavior but very thin for `occupancy.py`; over-mock smell: fake `display-message` rows only, no coverage of malformed rows, missing pane, live-agent path, dirty pane not considered by assert, or `looks_like_dispatch_launcher_payload`. |
| `test_tmuxctl_pane_select.py` | Pins absolute arrows for palace/somnium, absolute stack persona/worker extremes, no-op when no stack workers, absolute selection does not mutate focused-worker option, mechanicus left/right/up/down semantics, relative-right from persona to focused worker, and zoom restoration after absolute/relative selection when tmux drops native zoom. | Good behavior-level tests on `pane_select.py:310-433` with a stateful fake adapter. Still fake geometry: panes are synthetic; no real key-table/tmux hook integration. The stored-focused-worker path is covered only for relative-right, not stale/duplicate role fallbacks. |
| `test_tmuxctl_resolve_instance.py` | Pins `resolve_instance`: single global `list-panes -a` scan, skip unstamped panes, found/not-found shape, stamped pane without role returns no role, role canonicalization, and service JSON shape. | Real and focused. It intentionally pins current stamp-only resolver (`resolver.py:289-329`); this is likely future-drift territory because vault end-state moves identity facts off `@` stamps into daemon/token-api ledgers. |
| `test_tmuxctl_resolver_public.py` | Pins positional/public resolver: `somnium:NE` and `somnium:SE` numeric/page aliases resolve to actual right-grid panes rather than legacy-collapsed `N/S`; deprecated aliases (`TL/TR/BL/BR/NW/SW`) are rejected/miss. | Good regression for the canonical-id transition (`resolver.py:101-188`). Narrow: no duplicate-role ambiguity, physical-id session precedence, tombstone chain, or current-session/pinned-session behavior. |
| `test_tmuxctl_inspect_canonical.py` | Pins inspect render default as canonical-only for workspace/window/pane; `--physical` opt-in restores raw `%NN`; unset-role panes hide raw physical id by default. | Real render-level assertions on `inspect.py:27-58`, `202-238`. Good for no-leak contract. It does not test `render_doctor`, restart-plan/result renderers, stack suffix base handling, or CLI parser plumbing. |
| `test_tmuxctl_public_ids.py` | Pins public-id translation: map uses only live non-empty `@PANE_ID`, canonicalizes labels, omits unmapped/identity rows; raw `%NN` in text is replaced with public ids or `unresolved`, never passed through. | Strong pure-ish regression around `public_ids.py:11-55`. Gaps: regex only `%\d+`, no tests for punctuation/adjacent ids beyond simple text, no CLI `translate-ids` stdin path, no target resolver integration. |
| `test_tmuxctl_metal_resolver.py` | Pins metal resolver classification, process-tree search through wrappers/direct children, no match for bare shell, Claude transcript selection/fallback/missing project, Codex index selection/cwd filtering/mtime fallback/no match, pane resume resolution using agent cwd over pane cwd, non-resumable shells/unresolved agents, and session observation parsing. | High-value DB-free characterization of `metal_resolver.py`. It uses synthetic transcript stores/process tables, which is appropriate. Blind spots: malformed tmux `list-panes` rows crash today; permission/OS errors in process cwd/table are only partially modeled; no symlink/case-normalization path cases. |
| `test_tmuxctl_metal_restart.py` | Pins sandbox guard refusing `main` and sessions grouped with main, isolated sessions allowed, guard runs before observation, terminate SIGTERM/SIGKILL behavior with injectable sleep/table/kill, dry-run no kill/dispatch, execute kills engine processes not pane shells and dispatches DB-free, dispatch failures become failed rows, unresolvable agents not killed, render shape, and static no-DB import guard. | Good characterization for an experimental/sandbox path (`metal_restart.py`). Smell: `terminate_agent` contains polling/sleep loops in production (`metal_restart.py:98-119`); tests inject no-op sleep but do not bound real CLI behavior. Static DB-free test is string-level. |
| `test_tmuxctl_liveness.py` | Pins live TUI detection from pane pid/process subtree; bare shell/dead pane not live; `instance_live_tui` first checks resolved pane, then divergence-sweeps stamped panes for same instance, then returns none for idle husk. | Good model tests over `liveness.py` + patched `custodes._process_tree`; no live OS process integration. It pins process-sniff liveness as belt-and-suspenders, which may be demoted under daemon ledger architecture. |
| `test_tmuxctl_freelist.py` | Pins `list_free_panes`: clean + no instance + no live agent only; excludes `@INSTANCE_ID`; excludes live agent even missing stamp; canonicalizes optional roles; empty if none clean; single scan; service output shape; hard-excludes singleton labels even without stamp/agent. | Stronger than `test_tmuxctl_occupancy.py` for `occupancy.py`/`resolver.py:332+`. It still models process liveness by monkeypatching `_active_agent`; no daemon ledger state because none exists. Good pin for current reality. |
| `test_tmux_pane_label.py` | Pins retired `tmux-pane-label`: only `--backfill` works, seeding `@PANE_LABEL` for live DB rows with name and pane; stopped/empty rows excluded; legacy positional invocation fails loudly without shelling to tmux. | Real subprocess CLI test with fake tmux and sqlite. Good for retirement contract. It intentionally depends on old DB `instances` columns for deploy-time backfill; no tests for tmux command failures or duplicate panes. |
| `test_tmux_shim_id_sanitizer.py` | Pins shell shim sanitization: human-readable reads (`list-panes`, `list-windows`, `list-sessions`, capture, aliases) translate raw `%NN` to public ids/unresolved; programmatic pure `display-message -p '#{pane_id}'` passes raw through for target round-trip; embedded/human-facing display sanitizes. | Valuable end-to-end subprocess test of `bin/tmux` against fake real tmux. Brittle by shell `case "$*"` fake shapes; not real tmux. Good regression for over-translation bug. Gaps: env toggles (`IMPERIUM_TMUX_SANITIZE_IDS`, `IMPERIUM_TMUX_RAW`) and write/target-resolution interactions are barely covered. |

## 2. Coverage gaps — code behavior present, not pinned here

| Area | Untested behavior in code | Candidate regression test |
|---|---|---|
| Resolver tombstones | `resolve_pane_in_snapshot` follows tombstone chains, detects cycles, missing targets, and missing `@TOMBSTONE_TARGET` (`resolver.py:194-219`). | Characterize one valid chain, one cycle, one missing target. |
| Resolver session precedence | `resolve_pane` chooses physical target's own session, then explicit `session_name`, adapter `pinned_resolution_session`, then ambient current session (`resolver.py:222-255`). | Fake adapter/snapshot test that proves explicit/pinned session prevents resolving against ambient session. |
| Duplicate public roles | `_add_unique`/`dict.setdefault` make first-writer-wins for duplicate position/logical labels (`resolver.py:120-168`). | Pin current first-wins behavior or assert diagnostics if product wants fail-closed. |
| `resolve_to_public` / `resolve_to_physical` | Thin wrappers raise when a target has no public `@PANE_ID` or return physical id (`resolver.py:258-266`). | Add direct regression for no-role target failing with message. |
| Inspect doctor | `render_doctor` checks missing canonical windows, orphan transient windows, broken focus stash windows, transient grid/side markers, broken focus pane/stash size, window warnings, and tombstone target issues (`inspect.py:122-199`). | Table-driven doctor render tests for each warning family. |
| Restart rendering | `render_restart_plan` and `render_restart_result` expose coherence issues, clients, grouped sessions, resumes/skips/actions (`inspect.py:61-119`). | Snapshot-style render tests using model objects; useful because output is operator-facing. |
| Occupancy malformed/missing pane | `occupancy_for_pane` resolves logical target, returns `None` on missing/malformed display row, and `assert_dispatch_target_available` errors on missing pane/live-agent/instance (`occupancy.py:106-162`). | Regression for each fail-closed message path and malformed row. |
| Dispatch payload detection | `looks_like_dispatch_launcher_payload` treats `clear`, `dispatch-agent.`, and `TOKEN_API_INTERNAL_DISPATCH=1` as dispatch launcher payloads (`occupancy.py:165-169`). | Pure tests; likely used by send gate/dispatch detection. |
| TmuxAdapter runtime invariant cleanup | `run()` clears runtime state before `respawn-pane`; clearing `@INSTANCE_ID` clears pane style (`tmux_adapter.py:427-447`). | Fake raw tmux test proving respawn unsets runtime options/styles before command. |
| TmuxAdapter focus guard command matrix | Focus guard blocks foreground `split-window`/`new-window` into mechanicus, allows detached creation, allows title-only `select-pane -T`, and honors override env/options (`tmux_adapter.py:296-352`). | More command-matrix tests, especially split/new detached vs foreground. |
| Pane-select validation and stale focus | `select_pane` raises invalid mode/direction; `_stored_focused_worker` ignores stale/non-worker stored panes; `_stable_target_for` falls back to physical id on duplicate roles (`pane_select.py:231-275`, `399-433`). | Characterization tests for invalid args, stale stored worker, duplicate role fallback. |
| Focus refuse/noop paths | `plan_focus_grid` refuses second active grid focus, noops when active pane is not grid/no siblings; `plan_unfocus_grid` refuses missing focused pane, invalid stash entries, bad restore coordinate, missing stashed pane, wrong stash size; `plan_focus_side` noops non-side panes (`focus.py:128-330`). | Pure plan tests for each reason string. |
| Public id CLI/stdin path | `translate_stdin_to_stdout` and `tmuxctl translate-ids` surface are not pinned in this domain; only pure map/replace is tested (`public_ids.py:58-59`, `service.py:398+`, `cli.py` translate parser). | Subprocess/CLI test that raw ids in stdin become public/unresolved. |
| Metal resolver malformed external state | `observe_session` assumes exactly seven tab fields and int pane pid (`metal_resolver.py:338-367`); process-table/cwd error surfaces are only partly characterized. | Pin current crash or add bounty for fail-closed parse errors, depending intent. |
| Metal restart real-time bounds | `terminate_agent` has production polling up to 24s per pane before failure (`metal_restart.py:98-119`); tests inject sleep away. | Characterize timeout/escalation without wall-clock sleeps by injected monotonic or document as not testable until refactored. |
| `tmux-pane-label` failure surfaces | Backfill does not have tests for tmux failures, missing DB/table, duplicate panes, or status set beyond `idle/working/stopped`. | CLI tests for fail-open/fail-closed semantics. |
| Shell shim env controls | `bin/tmux` sanitizer env knobs (`IMPERIUM_TMUX_SANITIZE_IDS`, `IMPERIUM_TMUX_RAW`) and write-target resolution are not covered by sanitizer tests. | Subprocess tests for raw opt-out and non-read command pass-through. |

## 3. Vault features without tests — bounty candidates

| Vault ref | Feature intent | Current test coverage | Bounty candidate |
|---|---|---|---|
| `/Volumes/Imperium/Imperium-ENV/Mars/Tasks/agent-infra-adoption-and-front-door-lockdown.md` §4a | Standardize a generic **triple-ID runtime oracle**: almost all tmuxctld entry points should accept instance id, pane id, and singleton-persona id interchangeably. | Current tests cover separate surfaces: pane public ids, `resolve_instance`, and persona-ish labels, but not one generic oracle across tmuxctld entry points. | Bounty tests for daemon/runtime oracle accepting all three ID types and returning one canonical runtime target. |
| `/Volumes/Imperium/Imperium-ENV/Mars/Architecture/dispatch-split-brain-occupancy-ledger.md` §B1 | Daemon-owned persisted occupancy ledger with `SHIPPED → OPEN → CLOSED`; occupied is `SHIPPED|OPEN`; boot reconcile prunes dead panes. | Current tests only cover stamp/process/singleton-derived occupancy (`occupancy.py`) and freelist. No persisted ledger tests in this domain. | Bounty tests for tmuxctld ledger state machine, occupied semantics, and one-shot boot prune. |
| Same architecture doc §“Facts to migrate off @-stamps into the ledger” | Migrate `instance_id`, wrapper launch id, engine/launcher/persona/label/born epoch off pane `@` stamps into daemon ledger; tmux only supplies physical basement facts. | Tests pin `@INSTANCE_ID`, `@PANE_ID`, `@PANE_CLEAN` as current read sources (`test_resolve_instance`, `test_freelist`, `test_occupancy`). | Bounty tests for ledger-backed identity/occupancy reads that do not depend on pane `@INSTANCE_ID` for authoritative identity. |
| Same architecture doc §B3 | Split-brain dispatch: token-api creates pane-free contract; tmuxctld resolves pane/occupancy and ships via `respawn-pane`; readiness async via hooks; migrate resume/reservist/stack off `send_keys` into shell. | `metal_restart` currently pins DB-free dispatch command into same pane; no tmuxctld ship contract tests. | Bounty tests for daemon ship accepting a contract, resolving occupancy, using `respawn-pane`, and returning bare accepted response. |
| Same architecture doc §Open forks | WrapperEnd daemon close-ping must be async/fire-and-forget; `pane-died` is authoritative backstop. | No close/ledger tests here; focus/occupancy tests do not cover wrapper lifecycle. | Bounty tests that close ping does not stall and pane-died closes ledger row. |
| `/Volumes/Imperium/Imperium-ENV/Mars/Tasks/at-instance-id-empty-stamp-root-fix.md` canonical framing | Wrapper ping + SessionStart ping are minimum registration set; both ping both systems; wrapper authoritative for daemon, session authoritative for token-api; no pre-warm guarantee. | Current liveness/resolve tests assume stamps/process scan and do not test dual-ping ordering or missing prewarm. | Bounty tests for no-prewarm wrapper+session registration producing both daemon occupancy identity and token-api instance row. |
| Same task §Scope / singleton seats | Singleton/perpetual seats must reacquire valid identity and survive resume/restart churn; empty stamp/tint must not flap `council:custodes` routing. | Current tests hard-exclude singleton seats from freelist/dispatch and focus guard mechanicus; no resume-cycle singleton identity test. | Bounty tests for singleton seat identity re-stamp after resume/restart and stable persona routing. |
| `/Volumes/Imperium/Imperium-ENV/Mars/Bugs/codex-resume-rollout-id-orphans-instance-registration.md` §Fix (B2) | Token-api mints canonical `instance_id`; codex rollout id maps to canonical at SessionStart; resume cannot orphan original instance. | Metal resolver tests deliberately return Codex rollout ids from `~/.codex`; no canonical-id mapping tests. | Bounty tests for Codex resume mapping rollout id → canonical instance id and preserving pane/instance registration. |
| `/Volumes/Imperium/Imperium-ENV/Mars/Bugs/regression-dispatcher-targets-live-custodes-pane.md` §Fix | Free-seat / dispatch-target selection excludes panes with live working instance and hard-excludes singleton pane labels. | Partially covered now by `test_tmuxctl_freelist.py` and `test_tmuxctl_occupancy.py`. | Remaining bounty only if moved to daemon ledger: verify dispatcher/freelist consumes daemon occupancy, not just stamps/process scan. |
| `/Volumes/Imperium/Imperium-ENV/Ultramar/Session Propagation/discord-cadia-voice-routing.md` §Architectural Decisions | Public tmuxctl IDs/aliases are the voice routing contract; physical `%pane` IDs are volatile and resolved only at final operation time. | Generic public-id/inspect sanitizer tests exist, but no voice-routing/static persona route tests in this domain. | Bounty tests for static voice routes storing public ids and resolving physical ids only at final write. |
| `/Volumes/Imperium/Imperium-ENV/Mars/Bugs/dispatch-to-dead-pane-no-target-validation.md` info block | Daemon `respawn-pane` under split-brain contract should replace sending prompts into whatever shell is there; dead/wrong targets cannot silently swallow prompts. | No test in this domain asserts daemon dead-target validation; metal restart skips unresolvable agents but still dispatches through CLI for resolvable ones. | Bounty tests for daemon dispatch refusing vanished/wrong pane before prompt bytes are sent. |

## 4. Drift / contradictions — pin+bounty pair candidates

| Drift | Code/current behavior ref | Vault intent ref | Mutually-exclusive assertions to encode later |
|---|---|---|---|
| Occupancy source of truth is live tmux stamps/process/singleton labels, not persisted daemon ledger. | `occupancy.py` says occupancy is derived from live tmux pane state plus process-tree oracle, never DB (`tmuxctld/lib/tmuxctl/occupancy.py:1-7`); `PaneOccupancy.occupied` is `@INSTANCE_ID or live_agent or singleton` (`occupancy.py:33-41`); tests pin this in `test_tmuxctl_freelist.py` and `test_tmuxctl_occupancy.py`. | `dispatch-split-brain-occupancy-ledger.md` §B1: persisted tmuxctld sqlite ledger, `SHIPPED → OPEN → CLOSED`, occupied ≡ `SHIPPED|OPEN`, migrate facts off stamps. | Pin: a clean pane with empty `@INSTANCE_ID`, no live agent, non-singleton is dispatch-available because no live stamp/process marks it. Bounty: daemon ledger row `SHIPPED` or `OPEN` makes the pane occupied even if pane stamps/process scan look empty. |
| Instance resolution is stamp-only; vault wants canonical instance identity decoupled and ledger/token-api-backed. | `resolve_instance` single-scans `#{@INSTANCE_ID}` and skips unstamped panes (`resolver.py:289-329`); tests assert unstamped panes are skipped and not found. | `codex-resume-rollout-id-orphans-instance-registration.md` §Fix B2 and `dispatch-split-brain-occupancy-ledger.md` §B2: token-api mints canonical id; codex rollout maps to canonical; stamps are not the authoritative identity source. | Pin: `resolve_instance(adapter, iid)` returns not found when no live pane carries `@INSTANCE_ID == iid`. Bounty: runtime oracle resolves canonical `iid` via daemon/token-api mapping even when codex rollout/stamp differs or is absent but registration pair landed. |
| Metal restart relaunches via `dispatch --pane <%pane>` after killing engine process; vault wants tmuxctld ship via `respawn-pane` and migration off send-keys/shell dispatch paths. | `metal_restart.py` documents and implements in-place resume through DB-free dispatch CLI (`metal_restart.py:1-12`, `122-170`, `173-255`). | `dispatch-split-brain-occupancy-ledger.md` §B3: tmuxctld resolves pane + occupancy and ships via `respawn-pane`; migrate resume/reservist/stack off `send_keys`-into-shell. | Pin: metal restart resumes by invoking `bin/dispatch --id ... --engine ... --dir ... --pane %N` with `TOKEN_API_DB=/nonexistent/...`. Bounty: restart/resume ship path calls tmuxctld contract/`respawn-pane` and does not invoke dispatch in the pane/shell path. |
| Public/canonical pane identity still comes primarily from `@PANE_ID` stamps; vault says load-bearing identity facts should move off stamps into daemon ledger. | `resolver.py`, `inspect.py`, `public_ids.py`, and `pane_select.py` read `@PANE_ID` as public/canonical role (`resolver.py:87`, `137-139`; `inspect.py:38-47`, `228-230`; `public_ids.py:11-36`; `pane_select.py:135`). Tests strongly pin no-raw-id display. | `dispatch-split-brain-occupancy-ledger.md` §Facts to migrate off `@`-stamps: `instance_id`, wrapper id, engine/launcher/persona/label/born epoch migrate; `Ultramar/Tmuxctl Pane-Identity Sole-Authority` is cited as authority sibling. | Pin: public/canonical id map is built from live `@PANE_ID`; panes with no `@PANE_ID` are omitted or rendered `(unset)`. Bounty: daemon/runtime oracle provides the public identity/label for managed panes even when pane option is missing/stale, while raw tmux remains only basement physical state. |
| Singleton empty-stamp behavior is guarded at dispatch/freelist by hard-excluding singleton labels, not by fixing singleton registration survival. | `PaneOccupancy.occupied` treats singleton label alone as occupied (`occupancy.py:28-37`); tests assert singleton labels are excluded even without stamp/agent. | `at-instance-id-empty-stamp-root-fix.md` §Scope and canonical framing: singleton/perpetual seats must reacquire valid `@INSTANCE_ID`/identity across resume; empty stamp/tint is a tmux-side symptom to fix through wrapper/session pings. | Pin: `legion:custodes`/`mechanicus:fabricator-general` with empty `@INSTANCE_ID` is refused solely because it is a singleton label. Bounty: after wrapper/session registration, singleton seat has valid daemon identity/stamp/tint; guard no longer depends on empty-stamp singleton hard-exclusion as the only protection. |
| Focus/navigation is Python adapter + tmux hook/global-option based; vault front-door/daemon direction wants tmuxctld endpoints for agent plan/compact/context and broad runtime oracle. | `focus_guard.py` stores allow markers in tmux global options and logs files (`focus_guard.py:21-26`, `323-360`); `pane_select.py` calls `allow_human_focus` then selects panes directly (`pane_select.py:399-433`). | `agent-infra-adoption-and-front-door-lockdown.md` §2b and §4a: tmuxctld daemon endpoint for plan/compact, context-window query, and generic triple-ID runtime oracle across entry points. | Pin: pane-select directly mutates tmux focus and writes tmux global focus-allow options. Bounty: relevant agent-facing navigation/control endpoints route through tmuxctld and accept instance/pane/singleton IDs through the standardized oracle. |
| Shim sanitizer intentionally lets programmatic pure pane-id reads expose raw `%NN` for round-trip; vault voice/public-id doctrine says physical ids should not be stored and public ids are routing contract. | `test_tmux_shim_id_sanitizer.py` pins `display-message -p '#{pane_id}'` returning `%11`; `bin/tmux` supports raw programmatic capture. | `discord-cadia-voice-routing.md` §Architectural Decisions: physical `%pane` IDs are volatile and must be resolved only at final operation time; store public tmuxctl targets/aliases. | Pin: pure programmatic `display-message -p '#{pane_id}'` returns raw physical id so scripts can immediately reuse it as `-t`. Bounty: higher-level routing/storage APIs reject persisted raw `%pane` ids and store public targets, while still permitting final-operation raw resolution internally. |

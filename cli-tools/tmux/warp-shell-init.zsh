# warp-shell-init — Shell init for warp tmux panes
# Sourced automatically by tmux-workspace in warp window panes
# Works in both zsh (Mac) and bash (WSL)
#
# Lightweight init — the dirty-bit window rename system has been replaced
# by instance count display driven by Token-API events.

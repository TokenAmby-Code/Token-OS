#!/usr/bin/env python3
# timer-status - Quick timer status display
# TAGS: token-api
# AUDIENCE: human, agent
"""Quick timer status display.

Usage:
    timer-status                      # One-line status
    timer-status --layers             # One-line status + layer breakdown
    timer-status --balance            # Compact single-line: mode + colored balance only
    timer-status --shifts             # One-line status + session mode distribution + sparkline
    timer-status --sparkline          # One-line status + balance trajectory sparkline only
    timer-status --width N            # Set sparkline width (default 50); use with --shifts or --sparkline
    timer-status --watch              # Continuous monitoring (1s refresh)
    timer-status --json               # Raw JSON from API
"""

import os
import sys
import json
import time
import urllib.request

API_URL = os.environ.get("TOKEN_API_URL", "http://localhost:7777")

# v2 effective modes (lowercase from API)
MODE_ICONS = {
    "working":      "\U0001f4bc",   # 💼
    "multitasking": "\u26a1",       # ⚡
    "idle":         "\u23f8",       # ⏸
    "distracted":   "\u26a0\ufe0f", # ⚠️
    "break":        "\u2615",       # ☕
    "sleeping":     "\U0001f319",   # 🌙
}


def get_timer():
    try:
        req = urllib.request.Request(f"{API_URL}/api/timer", method="GET")
        with urllib.request.urlopen(req, timeout=2) as resp:
            return json.loads(resp.read().decode())
    except Exception as e:
        return None


def get_shifts():
    try:
        req = urllib.request.Request(f"{API_URL}/api/timer/shifts", method="GET")
        with urllib.request.urlopen(req, timeout=3) as resp:
            return json.loads(resp.read().decode())
    except Exception:
        return None


def fmt_time(seconds):
    """Format seconds as HH:MM:SS or MM:SS."""
    if seconds <= 0:
        return "00:00"
    h = seconds // 3600
    m = (seconds % 3600) // 60
    s = seconds % 60
    if h > 0:
        return f"{h}:{m:02d}:{s:02d}"
    return f"{m:02d}:{s:02d}"


def fmt_seconds_hm(seconds):
    """Format seconds as Xh Ym."""
    h = int(seconds) // 3600
    m = (int(seconds) % 3600) // 60
    if h > 0:
        return f"{h}h {m:02d}m"
    return f"{m}m"


def fmt_ms_hm(ms):
    """Format milliseconds as Xh Ym."""
    h = ms // (1000 * 60 * 60)
    m = (ms % (1000 * 60 * 60)) // (1000 * 60)
    return f"{h}h {m:02d}m"


def color(text, code):
    """ANSI color wrapper."""
    return f"\033[{code}m{text}\033[0m"


def display_balance(data):
    """Compact single-line: mode icon + name + colored break balance."""
    mode = (data.get("current_mode") or "").lower()
    icon = MODE_ICONS.get(mode, "\u2753")
    mode_name = mode.replace("_", " ").title() if mode else "?"

    if data.get("is_in_backlog"):
        bal_s = -round(data.get("break_backlog_ms", 0) / 1000)
    else:
        bal_s = data.get("accumulated_break_seconds", 0)

    abs_str = fmt_time(abs(bal_s))
    bal_str = ("+" if bal_s >= 0 else "-") + abs_str

    if bal_s > 1800:
        bal_colored = color(bal_str, "32")   # green
    elif bal_s > 0:
        bal_colored = color(bal_str, "33")   # yellow
    else:
        bal_colored = color(bal_str, "31")   # red

    print(f"{icon}  {mode_name}  {bal_colored}")


def display(data, show_layers=False):
    mode = (data.get("current_mode") or "").lower()
    icon = MODE_ICONS.get(mode, "\u2753")  # ❓ fallback
    mode_name = mode.replace("_", " ").title() if mode else "?"

    break_s = data.get("accumulated_break_seconds", 0)
    break_str = fmt_time(break_s)
    work_str = fmt_ms_hm(data.get("total_work_time_ms", 0))

    # Color break time
    if break_s > 1800:
        break_colored = color(break_str, "32")  # green
    elif break_s > 300:
        break_colored = color(break_str, "33")  # yellow
    else:
        break_colored = color(break_str, "31")  # red

    # Layer context: desktop_mode or phone_app label
    activity_detail = ""
    desktop_mode = data.get("desktop_mode")
    phone_app = data.get("phone_app")
    if desktop_mode and desktop_mode not in ("silence", "working"):
        activity_detail = color(f"  [{desktop_mode}]", "36")
    elif phone_app:
        activity_detail = color(f"  [phone:{phone_app}]", "36")

    # Manual mode / lock indicator
    lock = ""
    manual = data.get("manual_mode")
    if manual:
        lock = color(f"  [{manual}]", "33")
    elif data.get("manual_mode_lock"):
        lock = color("  [locked]", "33")

    # Backlog indicator
    backlog = ""
    if data.get("is_in_backlog"):
        bl_s = round(data.get("break_backlog_ms", 0) / 1000)
        backlog = color(f"  Backlog: {fmt_time(bl_s)}", "31")

    # Work mode (clocked out / gym)
    wm = data.get("work_mode", "clocked_in")
    wm_str = ""
    if wm == "clocked_out":
        wm_str = color("  OFF", "90")
    elif wm == "gym":
        wm_str = color("  GYM", "35")

    # Focus indicator
    focus_str = ""
    if data.get("focus_active"):
        focus_time = data.get("total_focus_time", "0h 0m")
        cutoff = data.get("focus_cutoff_time", "21:00")
        focus_str = color(f"  Focus: {focus_time} (off@{cutoff})", "35")

    print(f"{icon}  {mode_name}  Break: {break_colored}  Work: {work_str}{activity_detail}{lock}{backlog}{wm_str}{focus_str}")

    if show_layers:
        activity = data.get("activity", "?")
        prod_active = data.get("productivity_active", False)
        prod_str = color("active", "32") if prod_active else color("inactive", "90")
        manual_str = manual if manual else color("—", "90")
        dm_str = desktop_mode or color("—", "90")
        focus_on = data.get("focus_active", False)
        focus_layer = color("on", "35") if focus_on else color("off", "90")
        print(f"  Activity: {activity}  Desktop: {dm_str}  Productivity: {prod_str}  Manual: {manual_str}  Focus: {focus_layer}")


def sparkline(series, width=50):
    """Render a balance trajectory as a colored ASCII spark line.

    Each character maps to a sampled point. Positive balance = green,
    negative = red, zero = dim. Uses block chars ▁▂▃▄▅▆▇█ (8 levels).
    Returns the colored string + a brief label.
    """
    blocks = "▁▂▃▄▅▆▇█"
    if not series or len(series) < 2:
        return color("  (no data)", "90")

    # Downsample to `width` evenly-spaced points
    n = len(series)
    indices = [int(round(i * (n - 1) / (width - 1))) for i in range(width)]
    sampled = [series[i] for i in indices]

    lo = min(series)
    hi = max(series)
    span = hi - lo if hi != lo else 1.0

    bar_chars = []
    for v in sampled:
        level = int((v - lo) / span * 7)
        ch = blocks[level]
        if v > 0:
            bar_chars.append(color(ch, "32"))   # green
        elif v < 0:
            bar_chars.append(color(ch, "31"))   # red
        else:
            bar_chars.append(color(ch, "90"))   # dim

    current = series[-1]
    sign = "+" if current >= 0 else ""
    lo_sign = "+" if lo >= 0 else ""
    hi_sign = "+" if hi >= 0 else ""
    label = (color(f"{lo_sign}{lo:.0f}", "31") + "→" +
             color(f"{hi_sign}{hi:.0f}", "32") +
             "  now:" + (color(f"{sign}{current:.0f}", "32") if current >= 0 else color(f"{sign}{current:.0f}", "31")))

    return "  " + "".join(bar_chars) + "  " + label


def display_shifts(shifts, width=50):
    """Print a compact session mode-distribution summary."""
    dist = shifts.get("mode_distribution", {})
    total_s = sum(dist.values()) or 1

    # Mode order: productive → neutral → negative
    mode_order = ["working", "multitasking", "idle", "break", "distracted", "sleeping"]
    mode_colors = {
        "working":      "32",  # green
        "multitasking": "33",  # yellow
        "idle":         "90",  # dim
        "break":        "36",  # cyan
        "distracted":   "31",  # red
        "sleeping":     "34",  # blue
    }

    parts = []
    for m in mode_order:
        s = dist.get(m, 0)
        if s == 0:
            continue
        pct = 100.0 * s / total_s
        icon = MODE_ICONS.get(m, "?")
        label = fmt_seconds_hm(s)
        col = mode_colors.get(m, "37")
        parts.append(f"{icon} {color(label, col)} ({pct:.0f}%)")

    print("  " + "  ".join(parts))

    # Trigger / enforcement row
    triggers = shifts.get("shifts_by_trigger", {})
    enforced = shifts.get("enforcement_count", 0)
    twitter = shifts.get("twitter_shifts", 0)
    total_shifts = shifts.get("total_shifts", 0)

    trig_parts = []
    for k, v in sorted(triggers.items(), key=lambda x: -x[1]):
        if v > 0:
            trig_parts.append(f"{k}:{v}")

    trig_str = "  ".join(trig_parts) if trig_parts else "—"
    enforced_str = color(str(enforced), "31") if enforced > 0 else "0"
    twitter_str = color(str(twitter), "31") if twitter > 20 else str(twitter)

    print(f"  {color(str(total_shifts) + ' shifts', '90')}  " +
          color(f"Enforced:{enforced}  ", "31" if enforced > 50 else "37") +
          color(f"Twitter:{twitter}  ", "31" if twitter > 20 else "37") +
          color(f"[{trig_str}]", "90"))

    # Balance trajectory sparkline
    series = shifts.get("balance_series", [])
    if series:
        print(sparkline(series, width=width))


def main():
    args = sys.argv[1:]

    if "--help" in args or "-h" in args:
        print(__doc__.strip())
        return

    if "--json" in args:
        data = get_timer()
        if data:
            print(json.dumps(data, indent=2))
        else:
            print("Error: Could not reach Token-API", file=sys.stderr)
            sys.exit(1)
        return

    show_balance = "--balance" in args
    show_layers = "--layers" in args
    show_shifts = "--shifts" in args
    show_sparkline = "--sparkline" in args

    # Parse --width N
    spark_width = 50
    if "--width" in args:
        idx = args.index("--width")
        if idx + 1 < len(args):
            try:
                spark_width = max(10, int(args[idx + 1]))
            except ValueError:
                pass

    if "--watch" in args:
        try:
            while True:
                print("\033[2J\033[H", end="")  # clear screen
                data = get_timer()
                if data:
                    if show_balance:
                        display_balance(data)
                    else:
                        display(data, show_layers=show_layers)
                    if show_shifts or show_sparkline:
                        shifts = get_shifts()
                        if shifts:
                            if show_shifts:
                                display_shifts(shifts, width=spark_width)
                            elif show_sparkline:
                                series = shifts.get("balance_series", [])
                                if series:
                                    print(sparkline(series, width=spark_width))
                else:
                    print(color("Server unreachable", "31"))
                time.sleep(1)
        except KeyboardInterrupt:
            pass
        return

    data = get_timer()
    if data:
        if show_balance:
            display_balance(data)
        else:
            display(data, show_layers=show_layers)
        if show_shifts or show_sparkline:
            shifts = get_shifts()
            if shifts:
                if show_shifts:
                    display_shifts(shifts, width=spark_width)
                elif show_sparkline:
                    series = shifts.get("balance_series", [])
                    if series:
                        print(sparkline(series, width=spark_width))
                    else:
                        print(color("  (no balance data)", "90"))
            else:
                print(color("  (shifts unavailable)", "90"), file=sys.stderr)
    else:
        print(color("Server unreachable", "31"), file=sys.stderr)
        sys.exit(1)


if __name__ == "__main__":
    main()

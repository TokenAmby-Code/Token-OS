#!/usr/bin/env bash
# Convenience shim for followup command
# TAGS: workflow
# AUDIENCE: human
source "$(dirname "$(readlink -f "$0")")/../lib/nas-path.sh"
exec "$CLI_TOOLS/bin/cli-wrapper" followup "$@"

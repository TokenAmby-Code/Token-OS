#!/usr/bin/env bash
# Fetch and tail Cloud Run logs
# TAGS: deploy
# AUDIENCE: human, agent
set -euo pipefail
CLI_TOOLS_DIR="$(cd "$(dirname "$0")/.." && pwd)"
exec uv run --directory "$CLI_TOOLS_DIR" python -m cli_tools.cloud_logs.cli "$@"

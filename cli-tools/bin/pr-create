#!/usr/bin/env bash
# Compatibility shim. Use pr-step instead.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$(readlink -f "$0")")" && pwd)"
echo "[pr-create] deprecated: use 'pr-step' (or 'pr-step --force create' for the old escape hatch)." >&2
exec "$SCRIPT_DIR/pr-step" --force create "$@"

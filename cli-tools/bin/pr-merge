#!/usr/bin/env bash
# Compatibility shim. Use pr-step instead.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$(readlink -f "$0")")" && pwd)"
echo "[pr-merge] deprecated: use 'pr-step' (or 'pr-step --force merge' for the old escape hatch)." >&2
exec "$SCRIPT_DIR/pr-step" --force merge "$@"

#!/usr/bin/env bash
# The ephemeral side-channel is intentionally disabled. This retained command
# name is a fail-loud operator surface, not a compatibility path.
printf '%s\n' 'ethereal-prompt: ephemeral channel disabled by decree' >&2
exit 1

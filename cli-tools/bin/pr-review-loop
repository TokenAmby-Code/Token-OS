#!/usr/bin/env bash
# Compatibility shim. Use pr-step instead.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$(readlink -f "$0")")" && pwd)"
echo "[pr-review-loop] deprecated: use 'pr-step' (or 'pr-step --force review' for the old escape hatch)." >&2
exec "$SCRIPT_DIR/pr-step" --force review "$@"
